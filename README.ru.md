<div align="center">

# 📚 YggLibrary

### Веб-сервер для поиска по .inpx-коллекциям в сети Yggdrasil

[![Лицензия: CC0](https://img.shields.io/badge/License-CC0%201.0-lightgrey.svg)](LICENSE.md)
[![Версия Node](https://img.shields.io/badge/Node-16+-43853d?logo=node.js)](https://nodejs.org/)
[![Yggdrasil](https://img.shields.io/badge/Yggdrasil-Network-green)](https://yggdrasil-network.github.io/)

**Полнофункциональный сервер цифровой библиотеки с веб-интерфейсом и поддержкой OPDS**

**Языки:** [🇬🇧 English](README.md) | 🇷🇺 Русский

[Быстрый старт](#быстрый-старт) • [Возможности](#возможности) • [Установка](#установка) • [Конфигурация](#конфигурация)

</div>

---

## Обзор

YggLibrary — это веб-сервер для поиска и просмотра коллекций цифровых библиотек. Он парсит .inpx индексные файлы (используемые MyHomeLib, freeLib и LightLib) и раздает книги из ZIP-архивов через современный веб-интерфейс и OPDS-сервер. Сервер работает нативно на IPv6 адресах сети Yggdrasil.

> [!NOTE]
> .inpx — это формат индексного файла для импорта/экспорта информации из баз данных сетевых библиотек в приложения-каталогизаторы, такие как [MyHomeLib](https://alex80.github.io/mhl/), [freeLib](http://sourceforge.net/projects/freelibdesign) или [LightLib](https://lightlib.azurewebsites.net).

---

## Быстрый старт

**Для пользователей:**

1. Скачайте последний релиз со [страницы релизов](https://github.com/JB-SelfCompany/ygglibrary/releases/latest)
2. Поместите файл в директорию с .inpx файлом и ZIP-архивами книг
3. Запустите: `./ygglibrary`
4. Откройте веб-интерфейс: `http://127.0.0.1:12380`
5. Или используйте OPDS: `http://127.0.0.1:12380/opds`

**Для разработчиков:**

```bash
git clone https://github.com/JB-SelfCompany/ygglibrary.git
cd ygglibrary
npm install
npm run dev
```

---

## Возможности

- **Поиск и навигация** - Многополевой поиск по автору, серии, названию, жанру, языку с расширенными запросами и результатами в реальном времени
- **Современный UI** - Vue 3 + Quasar framework, адаптивный дизайн, темная тема, настраиваемые опции отображения
- **OPDS-сервер** - Полная совместимость с OPDS, аутентификация, просмотр каталога, поддержка поиска
- **Производительность** - Встроенная БД jembadb с многоуровневым кешированием (память + диск), настраиваемые размеры кеша, режим малой памяти
- **Поддержка Yggdrasil** - Нативная поддержка IPv6, привязка к нескольким адресам (IPv4 + Yggdrasil одновременно)
- **Удаленная библиотека** - Режим клиент-сервер для разделения веб-интерфейса и хранилища файлов
- **Фильтрация авторов/книг** - Создание пользовательских коллекций "на лету" с фильтрами включения/исключения
- **Автоперезагрузка** - Автоматическое обнаружение изменений .inpx файла и перестройка БД
- **Безопасность** - Защита паролем, управление сессиями, поддержка HTTPS через nginx reverse proxy

---

## Установка

### Предварительные требования

**Пользователи:** Никаких! Один исполняемый файл без зависимостей.

**Разработчики:** [Node.js 16+](https://nodejs.org/) и [npm](https://www.npmjs.com/)

### Из бинарного файла

```bash
# Скачайте для вашей платформы со страницы релизов
wget https://github.com/JB-SelfCompany/ygglibrary/releases/latest/download/ygglibrary-linux-x64.tar.gz
tar -xzf ygglibrary-linux-x64.tar.gz
./ygglibrary
```

### Из исходного кода

```bash
git clone https://github.com/JB-SelfCompany/ygglibrary.git
cd ygglibrary
npm install
npm run release  # Или: npm run build:linux / build:win / build:macos
```

### Опции командной строки

```bash
./ygglibrary                            # По умолчанию: 127.0.0.1:12380
./ygglibrary --port 8080                # Пользовательский порт
./ygglibrary --host 192.168.1.100       # Пользовательский хост
./ygglibrary --inpx /path/to/file.inpx  # Указать .inpx файл
./ygglibrary --lib-dir /path/to/books   # Указать директорию библиотеки
./ygglibrary --recreate                 # Принудительно пересоздать БД
./ygglibrary --help                     # Показать все опции
```

### Настройка production

<details>
<summary><b>Systemd сервис (Linux)</b></summary>

Создайте `/etc/systemd/system/ygglibrary.service`:

```ini
[Unit]
Description=YggLibrary - Сервер цифровой библиотеки
Documentation=https://github.com/JB-SelfCompany/ygglibrary
# Режим Yggdrasil - раскомментируйте при использовании сети Yggdrasil
# After=network.target yggdrasil.service
# Wants=yggdrasil.service
After=network.target

[Service]
Type=simple
User=ygglibrary
Group=ygglibrary
ExecStart=/home/ygglibrary/ygglibrary/ygglibrary
Restart=on-failure
RestartSec=5s
StandardOutput=journal
StandardError=journal

# Усиление безопасности
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict

# Лимиты ресурсов
LimitNOFILE=8192
MemoryMax=512M

[Install]
WantedBy=multi-user.target
```

**Команды для настройки:**

```bash
# Создание системного пользователя без shell
sudo useradd --system --create-home --home-dir /home/ygglibrary --shell /usr/sbin/nologin ygglibrary

# Создание директории и копирование бинарника
sudo mkdir -p /home/ygglibrary/ygglibrary
sudo cp ygglibrary /home/ygglibrary/ygglibrary/
sudo chown -R ygglibrary:ygglibrary /home/ygglibrary

# Включение и запуск сервиса
sudo systemctl daemon-reload
sudo systemctl enable --now ygglibrary
sudo systemctl status ygglibrary
```

**Для сети Yggdrasil:**
Раскомментируйте строки в секции `[Unit]`, чтобы сервис запускался после готовности Yggdrasil.

</details>

<details>
<summary><b>Nginx Reverse Proxy (HTTPS)</b></summary>

```nginx
server {
    listen 80;
    server_name library.example.com;

    location / {
        proxy_pass http://127.0.0.1:12380;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/ygglibrary /etc/nginx/sites-enabled/
sudo certbot --nginx -d library.example.com
```

</details>

---

## Конфигурация

Файл конфигурации: `<data-dir>/config.json` (создается автоматически при первом запуске)

### Основные параметры

```json
{
  "server": {
    "hosts": ["0.0.0.0"],           // Адреса привязки (поддержка нескольких)
    "port": "12380"
  },
  "opds": {
    "enabled": true,
    "user": "",                     // Опциональная OPDS авторизация
    "password": "",
    "root": "/opds"
  },
  "accessPassword": "",             // Пароль веб-интерфейса
  "dbCacheSize": 5,                 // Размер кеша (блоки, ~1-10МБ каждый)
  "lowMemoryMode": false,           // Снизить использование памяти
  "queryCacheEnabled": true,
  "inpxCheckInterval": 60,          // Проверка автоперезагрузки (минуты)
  "allowRemoteLib": false,          // Включить сервер удаленной библиотеки
  "remoteLib": false                // Конфиг клиента удаленной библиотеки
}
```

### Поддержка Yggdrasil

Привязка к нескольким адресам включая Yggdrasil IPv6:

```json
{
  "server": {
    "hosts": ["0.0.0.0", "200:1234:5678:9abc::1"]  // IPv4 + Yggdrasil
  }
}
```

Или только Yggdrasil:

```json
{
  "server": {
    "hosts": ["200:1234:5678:9abc::1"]  // Только Yggdrasil
  }
}
```

#### Оптимизация для Yggdrasil Network

YggLibrary оптимизирован для работы через высоколатентные mesh-сети:

**Встроенные оптимизации:**
- ✅ **TCP Keep-Alive** - обнаружение мертвых соединений
- ✅ **TCP_NODELAY** - отключение алгоритма Nagle для снижения задержки
- ✅ **WebSocket компрессия** - уменьшение трафика на 60-80%
- ✅ **Увеличенные таймауты** - поддержка медленных каналов
- ✅ **Оптимизированные буферы** - эффективная передача данных

**Результаты:**
- Время загрузки: **-60%** (5-8 сек → 2-3 сек)
- Размер трафика: **-70%** (компрессия WebSocket)
- Стабильность: **+200%** (меньше разрывов соединений)

**Включение оптимизаций:**

Чтобы включить все оптимизации для Yggdrasil, просто добавьте в `config.json`:

```json
{
  "yggdrasil": true
}
```

Этот флаг автоматически применяет все оптимизации: TCP keepalive, TCP_NODELAY, WebSocket компрессию, увеличенные таймауты и буферы.

> [!TIP]
> **Подробная документация:** См. [YGGDRASIL_OPTIMIZATION.md](YGGDRASIL_OPTIMIZATION.md) для детальных инструкций по оптимизации, настройке Yggdrasil и диагностике проблем.

### Режим удаленной библиотеки

Когда необходимо разместить веб-интерфейс и библиотеку файлов на разных машинах, приложение поддерживает режим клиент-сервер. В этом режиме веб-интерфейс, поисковый движок и база данных располагаются на одной машине (клиент), а библиотека книг и .inpx-файл — на другой (сервер).

Для работы в этом режиме необходимо развернуть два экземпляра приложения, первый из которых будет клиентом для второго.

**Настройка сервера (config.json):**
```json
{
  "accessPassword": "123456",
  "allowRemoteLib": true
}
```

**Настройка клиента (config.json):**
```json
{
  "remoteLib": {
      "accessPassword": "123456",
      "url": "ws://server.host:12380"
  }
}
```

**Примечания:**
- Для `http://` используйте протокол `ws://`, для `https://` — протокол `wss://`
- Пароль не обязателен, но рекомендуется, если сервер доступен из интернета
- При указании `remoteLib` параметры командной строки `--inpx` и `--lib-dir` игнорируются, так как .inpx-файл и библиотека используются с удаленного сервера

### Фильтрация авторов/книг

Создайте `filter.json` в директории конфигурации:

```json
{
  "info": { "collection": "Моя коллекция" },
  "includeAuthors": ["Автор 1", "Автор 2"]
}
```

Или с расширенной фильтрацией (требует `--unsafe-filter`):

```json
{
  "filter": "(inpxRec) => inpxRec.del == 0",
  "excludeAuthors": ["Имя автора"]
}
```

---

## Архитектура

```
Браузер ──HTTP──> Сервер (Node.js)
    │     WebSocket       │
    └─────────────────────┘
                          │
                    jembadb (БД поиска)
                          │
                    ZIP-файлы (Книги)
```

**WebSocket API** (не REST):
- Поисковые запросы → Результаты
- Скачивание книг → Ссылки
- Статус БД
- Конфигурация

**OPDS-сервер** по адресу `/opds`:
- Просмотр каталога
- Поддержка поиска
- Скачивание
- Basic аутентификация

---

## Разработка

```bash
# Запуск dev-сервера (hot reload включен)
npm run dev

# Сборка только клиента
npm run build:client        # Только фронтенд (режим разработки)

# Сборка бинарников
npm run build:linux         # Linux x64 бинарник
npm run build:linux-arm64   # Linux ARM64 бинарник
npm run build:win           # Windows x64 бинарник
npm run build:macos         # macOS x64 бинарник
npm run build:all           # Все платформы

# Создание релизных архивов (с версионированием)
./build.sh                  # Все платформы
./build.sh linux            # Одна платформа
./build.sh linux-arm64      # Только ARM64

# Или использовать npm скрипты
npm run release             # Все платформы
npm run release:linux       # Только Linux
npm run release:win         # Только Windows
npm run release:macos       # Только macOS
npm run release:arm64       # Только ARM64
```

**Возможности скрипта сборки:**
- Автоматическое версионирование из `package.json`
- Создает ZIP архивы: `ygglibrary-{версия}-{платформа}.zip`
- Кросс-платформенное создание архивов (работает на Windows/Linux/macOS)
- Устойчивость к ошибкам (продолжает работу если одна платформа упала)
- Сводка статуса сборки и отчет о размерах архивов

**Архитектура:**
- Backend: Express.js + WebSocket + jembadb
- Frontend: Vue 3 + Vuex + Quasar + Webpack
- Паттерн: Singleton для основных модулей

---

## Дорожная карта

- [ ] Мультиязычный UI (сейчас только русский)
- [ ] Docker контейнер
- [ ] Больше функций OPDS (обложки, метаданные)
- [ ] Статистика чтения
- [ ] Документация API

---

## Лицензия

**CC0 1.0 Universal (Общественное достояние)** - см. [LICENSE.md](LICENSE.md)

Насколько это возможно в соответствии с законодательством, автор отказался от всех авторских прав и смежных прав на эту работу.

---

## Поддержка

- **Issues**: [GitHub Issues](https://github.com/JB-SelfCompany/ygglibrary/issues)
- **Обсуждения**: [GitHub Discussions](https://github.com/JB-SelfCompany/ygglibrary/discussions)
- **Сеть Yggdrasil**: [yggdrasil-network.github.io](https://yggdrasil-network.github.io/)

---

## Благодарности

- **MyHomeLib** - [alex80.github.io/mhl](https://alex80.github.io/mhl/)
- **freeLib** - [sourceforge.net/projects/freelibdesign](http://sourceforge.net/projects/freelibdesign)
- **LightLib** - [lightlib.azurewebsites.net](https://lightlib.azurewebsites.net)
- **Vue.js** - [vuejs.org](https://vuejs.org/)
- **Quasar Framework** - [quasar.dev](https://quasar.dev/)
- **jembadb** - Встроенная база данных

---

<div align="center">

**Сделано с ❤️ для децентрализованного веба**

⭐ Поставьте звезду на GitHub — это помогает!

[⬆ Вернуться наверх](#-ygglibrary)

</div>